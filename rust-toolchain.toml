[toolchain]
channel = "1.92"
# rustfmt and clippy are needed by CI (GitHub runners install rustup with the minimal
# profile); rust-analyzer keeps editors on a version matching this channel.
components = ["rustfmt", "clippy", "rust-analyzer"]
